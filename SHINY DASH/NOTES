# Sidebar
tags$img(src = "LOGOSHINY.png", width = "100%", height = "auto")

  # ------------
  # [2.4] RODAPÉ
  # ------------
  Footer <- dashboardFooter(
    left = "Dashboard criado via Pacote Shiny em R", 
    right = "Elaborado por Breno C R Silva"
  )
  
